/** Epsilon symbol for empty/epsilon transitions (Sipser convention) */
export const EPSILON = "ε";

/** Blank symbol for Turing machine tape */
export const BLANK = "☐";

/** Initial stack symbol for pushdown automata (Sipser convention: $) */
export const INITIAL_STACK = "$";

/** Unknown/placeholder symbol */
export const UNKNOWN = String.fromCharCode(0xfffd);
