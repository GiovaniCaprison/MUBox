{
  "name": "@mubox/local-tsflap",
  "version": "1.0.0",
  "description": "Finite Automata visualization and design tool for React",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsdown",
    "clean": "rm -rf dist coverage node_modules",
    "fix": "prettier . --write && eslint . --fix",
    "lint": "eslint .",
    "dev": "vite",
    "test": "vitest run",
    "type-check": "tsc"
  },
  "nx": {
    "targets": {
      "build": {
        "dependsOn": [
          "^build"
        ],
        "outputs": [
          "{workspaceRoot}/build/ui"
        ]
      },
      "fix": {
        "dependsOn": [
          "^build"
        ]
      },
      "lint": {
        "dependsOn": [
          "^build"
        ]
      },
      "test": {
        "dependsOn": [
          "^build"
        ]
      },
      "type-check": {
        "dependsOn": [
          "^build"
        ]
      }
    }
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/react": "^19.1.9",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.0"
  }
}
